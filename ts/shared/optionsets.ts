export const enum StateCode {
    Active = 0,
    Inactive = 1,
}

export const enum AccountStatusCode {
    Active = 0,
    Inactive = 1,
    Cancelled = 2,
}

export const enum AccountRelationshipTypes {
    Client = 3,
    Influencer = 6,
    Other = 12,
    Prospect = 8,
    Supplier = 10,
}

export const enum ActivityStateCode {
    Open = 0,
    Completed = 1,
    Cancelled = 2,
    Scheduled = 3,
}
